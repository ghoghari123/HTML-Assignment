let n = 20;
console.log(n);
    
const name = "HemuDelu"
console.log(name);

// ARITHMATIC OPERATIONS
var a , b
a = 20;
b = 90;
c = a + b;
d = a - b;
e = a * b;
f = a / b;
g = a % b;
console.log(c,d,e,f,g);

// ================================== DATA TYPES ================================================
console.log("DATA TYPES");

// ---------- [1] NUMBER
let num = 108;
console.log("num",num,typeof(num));

// ---------- [1] BOOLEANS
let value = true;
console.log("value",value,typeof(value));

// ---------- [1] STRING
let n_a_m_e= "RAM_SITA";
console.log("n_a_m_e",n_a_m_e,typeof(n_a_m_e));

// ---------- [1] NULL
let h1 = null;
console.log("h1",h1,typeof(h1));
// ---------- [1] UNDEFINE
let b1;
console.log("b1",b1,typeof(b1));

// ---------- [1] OBJECT
let string ={
    na_me : "radhe_radhe"
}
console.log("string",string,typeof(string));

// DOM manipulation is the process of using JS to dynamically change the structure,style, and content of a webpage. When a browser loads HTML, it creates a  Document Object Model [DOM]  - a tree-like representation of the page where every element, attribute and price of text is a 'noide' that can be modified.

// [1] Selecting Elements
// to manipulate an element , you must "find" or select it from the DOM tree. Modern developers primarily use these methodes provided by the MDN Web Docs.

// document.getElemenById('id')
// document.querySelector('.class')
// document.querySelectorAll('.class')
// document.getElemenByClassName('name')


